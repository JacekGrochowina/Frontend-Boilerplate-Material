export interface ILoginResponse {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  accessToken: string;
  refreshToken: string;
  createdDate: string;
  updatedDate: string;
}
